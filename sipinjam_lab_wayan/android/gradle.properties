org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=2G -XX:+HeapDumpOnOutOfMemoryError -Djava.net.preferIPv4Stack=true -Dhttps.protocols=TLSv1.2
android.useAndroidX=true
android.enableJetifier=true
# Works around a Kotlin incremental-compiler crash ("this and base files have
# different roots") that happens on Windows when the project and the pub
# cache live on different drive letters.
kotlin.incremental=false
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
